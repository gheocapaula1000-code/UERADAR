{
  "name": "UEradar PWA",
  "user": "ubuntu",
  "install": "export BUN_INSTALL=\"$HOME/.bun\"; export PATH=\"$BUN_INSTALL/bin:$PATH\"; command -v bun >/dev/null 2>&1 || curl -fsSL https://bun.sh/install | bash; bun install",
  "terminals": [
    {
      "name": "dev",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\"; bun run dev"
    }
  ],
  "ports": [{ "name": "dev", "port": 8080 }]
}
